{{ if ne .chezmoi.os "windows" -}}
#!/bin/sh
# ランチャー。本体は `scripts/generate-shims.sh`。
# テンプレート構文を含むとshfmt/shellcheckがパースできないため、シェルコードは
# 素の `.sh` に置いて既存のlintゲートに乗せ、ここには分岐だけを残す。
#
# `run_after_` は毎回のapplyで走るのでハッシュによる再実行判定は不要。
exec sh "{{ .chezmoi.sourceDir }}/scripts/generate-shims.sh"
{{ end -}}
