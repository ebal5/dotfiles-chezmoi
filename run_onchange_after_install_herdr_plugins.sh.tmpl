{{ if ne .chezmoi.os "windows" -}}
#!/bin/sh
# ランチャー。本体は `scripts/install-herdr-plugins.sh`。
# テンプレート構文を含むとshfmt/shellcheckがパースできないため、シェルコードは
# 素の `.sh` に置いて既存のlintゲートに乗せ、ここには分岐だけを残す。
#
# `run_onchange_` はこのファイルのレンダリング結果のハッシュで再実行を判定する。
# 本体と定義ファイルの両方のハッシュを埋めないと、それらを編集しても再実行されない。
# body: {{ include "scripts/install-herdr-plugins.sh" | sha256sum }}
# definitions: {{ include "dot_config/herdr-plugins" | sha256sum }}
exec sh "{{ .chezmoi.sourceDir }}/scripts/install-herdr-plugins.sh"
{{ end -}}
